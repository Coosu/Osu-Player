<Styles xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <Design.PreviewWith>
        <Border Padding="20">
            <StackPanel Spacing="20">
                <Slider Width="300" Value="50" Maximum="100" Classes="PinkSlider" Orientation="Horizontal" />
                <Slider Height="150" Value="50" Maximum="100" Classes="WhiteSlider" Orientation="Vertical" />
            </StackPanel>
        </Border>
    </Design.PreviewWith>
    
    <!-- 粉色滑块样式 -->
    <Style Selector="Slider.PinkSlider">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Value" Value="0" />
        <Setter Property="CornerRadius" Value="2" />
    </Style>
    
    <!-- 粉色水平 DecreaseButton -->
    <Style Selector="Slider.PinkSlider:horizontal /template/ RepeatButton#PART_DecreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Border Height="5" 
                            Background="White"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <Border.Transitions>
                            <Transitions>
                                <BrushTransition Property="Background" Duration="0:0:0.2" />
                            </Transitions>
                        </Border.Transitions>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2" OffsetY="1" Color="#FF9D9D" />
                        </Border.Effect>
                    </Border>
                    <Border Height="5" 
                            Background="#FF9D9D"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 粉色垂直 DecreaseButton -->
    <Style Selector="Slider.PinkSlider:vertical /template/ RepeatButton#PART_DecreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Border Width="5" 
                            Background="White"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <Border.Transitions>
                            <Transitions>
                                <BrushTransition Property="Background" Duration="0:0:0.2" />
                            </Transitions>
                        </Border.Transitions>
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2" OffsetX="1" Color="#FF9D9D" />
                        </Border.Effect>
                    </Border>
                    <Border Width="5" 
                            Background="#FF9D9D"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 粉色水平 IncreaseButton -->
    <Style Selector="Slider.PinkSlider:horizontal /template/ RepeatButton#PART_IncreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Height="3" 
                        Background="#EAEAEA"
                        CornerRadius="{TemplateBinding CornerRadius}" />
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 粉色垂直 IncreaseButton -->
    <Style Selector="Slider.PinkSlider:vertical /template/ RepeatButton#PART_IncreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Width="3" 
                        Background="#EAEAEA"
                        CornerRadius="{TemplateBinding CornerRadius}" />
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 滑块拇指样式 -->
    <Style Selector="Slider.PinkSlider /template/ Thumb#PART_Thumb">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Ellipse Width="15" 
                             Height="15" 
                             Fill="White">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.2" OffsetY="1" Color="#FF9D9D" />
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="15" 
                             Height="15" 
                             Fill="#FF9D9D"
                             Stroke="White"
                             StrokeThickness="2" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 鼠标悬停时的样式 -->
    <Style Selector="Slider.PinkSlider:pointerover /template/ Thumb#PART_Thumb">
        <Setter Property="Cursor" Value="Hand" />
    </Style>
    
    <!-- 白色滑块样式 -->
    <Style Selector="Slider.WhiteSlider">
        <Setter Property="Focusable" Value="False" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Value" Value="0" />
        <Setter Property="CornerRadius" Value="2" />
    </Style>
    
    <!-- 白色水平 DecreaseButton -->
    <Style Selector="Slider.WhiteSlider:horizontal /template/ RepeatButton#PART_DecreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Border Height="4" 
                            Background="White"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.3" OffsetY="1" Color="#FFFD629A" />
                        </Border.Effect>
                    </Border>
                    <Border Height="4" 
                            Background="#FFFD629A"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 白色垂直 DecreaseButton -->
    <Style Selector="Slider.WhiteSlider:vertical /template/ RepeatButton#PART_DecreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Border Width="4" 
                            Background="White"
                            CornerRadius="{TemplateBinding CornerRadius}">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="10" Opacity="0.3" OffsetX="1" Color="#FFFD629A" />
                        </Border.Effect>
                    </Border>
                    <Border Width="4" 
                            Background="#FFFD629A"
                            CornerRadius="{TemplateBinding CornerRadius}" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 白色水平 IncreaseButton -->
    <Style Selector="Slider.WhiteSlider:horizontal /template/ RepeatButton#PART_IncreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Height="3" 
                        Background="#80B0B0B0"
                        CornerRadius="{TemplateBinding CornerRadius}" />
            </ControlTemplate>
        </Setter>
    </Style>
    
    <!-- 白色垂直 IncreaseButton -->
    <Style Selector="Slider.WhiteSlider:vertical /template/ RepeatButton#PART_IncreaseButton">
        <Setter Property="Template">
            <ControlTemplate>
                <Border Width="3" 
                        Background="#80B0B0B0"
                        CornerRadius="{TemplateBinding CornerRadius}" />
            </ControlTemplate>
        </Setter>
    </Style>
    
    <Style Selector="Slider.WhiteSlider /template/ Thumb#PART_Thumb">
        <Setter Property="Template">
            <ControlTemplate>
                <Grid>
                    <Ellipse Width="11" 
                             Height="11" 
                             Fill="White"
                             Stroke="White"
                             StrokeThickness="3">
                        <Ellipse.Effect>
                            <DropShadowEffect BlurRadius="5" Opacity="0.3" OffsetY="1" Color="#FFFD629A" />
                        </Ellipse.Effect>
                    </Ellipse>
                    <Ellipse Width="11" 
                             Height="11" 
                             Fill="#FFFD629A"
                             Stroke="White"
                             StrokeThickness="3" />
                </Grid>
            </ControlTemplate>
        </Setter>
    </Style>
    
    <Style Selector="Slider.WhiteSlider:pointerover /template/ Thumb#PART_Thumb">
        <Setter Property="Cursor" Value="Hand" />
    </Style>
</Styles>